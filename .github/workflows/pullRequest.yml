name: Pull Request II

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: swift build
